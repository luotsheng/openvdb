import {
    StartNode,
    EndNode,
    ActionNode,
    BranchNode
} from "./FlowNodeBase";

export const nodetypes = {
    start: StartNode,
    end: EndNode,
    action: ActionNode,
    branch: BranchNode
};